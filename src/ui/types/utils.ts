import { BorderRadius, BorderRadiusValues, Spacing, SpacingValues } from './theme';

// Função para criar spacing com valores nomeados
export function createSpacing(values: SpacingValues): Spacing {
  return values;
}

// Função para criar border radius com valores nomeados
export function createBorderRadius(values: BorderRadiusValues): BorderRadius {
  return values;
}
